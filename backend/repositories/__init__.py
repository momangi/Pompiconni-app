"""Data access layer.

Each repository encapsulates the MongoDB queries for one domain. Repositories
do not contain business logic and do not orchestrate other repositories —
that belongs in ``services/``.

Refactor Fase 4B Batch 1 — extracted from ``server.py``.
"""
